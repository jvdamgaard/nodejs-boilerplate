describe('Mocha and Expectjs', function () {
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
});

describe('Mocha and Expectjs2', function () {
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
});

describe('Mocha and Expectjs3', function () {
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
  it('should be running', function () {
    expect(true).to.be(true);
  });
});